{
  "name": "ridge-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "editor": "npm run start --workspace=ridge-editor",
    "runtime": "npm run runtime --workspace=ridge-runtime",
    "build-editor": "npm run build --workspace=ridge-editor"
  },
  "workspaces": [
    "editor",
    "components/*",
    "server/*",
    "packages/*"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "standard": "^17.0.0"
  },
  "browserslist": [
    "last 1 chrome version",
    "last 1 firefox version"
  ]
}
